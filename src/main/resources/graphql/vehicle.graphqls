extend type Mutation {
    addVehicle(vehicleInput: VehicleInput): Vehicle
}

extend type Query {
    vehicleById(id: ID): Vehicle
    vehicles: [Vehicle]
}

type Vehicle {
    id: ID
    brand: String
    model: String
    color: String
    plate: String
    type: VehicleType
}

input VehicleInput {
    brand: String!
    model: String!
    color: String!
    plate: String!
    type: VehicleType!
}

enum VehicleType {
    CAR,
    MOTORCYCLE
}

